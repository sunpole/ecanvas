// scripts/dev-temp.js

// В этом файле размещается временный тестовый код, прототипы и эксперименты!
// Все функции — заглушки, можно смело расширять или удалять их позже.

/**
 * Пример временной тестовой функции.
 */
export function tempTestFunction() {
  console.log('[DEV-TEMP] tempTestFunction вызвана');
  return 'test';
}

/**
 * Прототип функции для ручного запуска тестов в процессе разработки.
 */
export function runDevPrototype() {
  console.log('[DEV-TEMP] runDevPrototype: запускается прототип');
  // Здесь может быть любой ваш временный код для отладки
}

/**
 * Заглушка под временные глобальные переменные, если потребуется (не рекомендуется постоянно!)
 */
export const devTempState = {};

console.log('[DEV-TEMP] dev-temp.js загружен');

// Добавляйте свои временные функции и переменные выше этого комментария
